# Optional: Set this to your backend server URL in production (e.g. on Vercel / Netlify)
# If frontend and backend are served from the same domain or using Vite proxy in development, leave empty.
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
